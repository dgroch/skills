# Conversion Intelligence Programme — Python deps (headless VPS run)
google-analytics-data>=0.18.0   # GA4 Data API (read-only)
google-auth>=2.27.0             # service-account credentials
requests>=2.31.0               # Shopify / Mida / Notion REST
python-dotenv>=1.0.0            # optional: load config/.env (loop has a fallback loader)
